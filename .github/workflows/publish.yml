name: Publish

on:
  workflow_call:
    secrets:
      NPM_TOKEN:
        required: true
        
# Limit concurrent jobs to avoid semantic-release failing because of out-of-date remote.
concurrency:
  group: ${{ github.workflow }}

jobs:
  publish:
    strategy:
      fail-fast: false
      matrix:
        package:
          - test
          - test-viem
          - test-ethers
          - test-wagmi

          - morpho-ts
          - morpho-test

          - blue-api-sdk
          - blue-sdk
          - blue-sdk-ethers
          - blue-sdk-viem
          - blue-sdk-wagmi

          - simulation-sdk
          - simulation-sdk-wagmi
          - bundler-sdk-viem

          - liquidation-sdk-viem

    name: "@morpho-org/${{ matrix.package }}"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install

      - run: |
          release_config=$(jq -r '.release' package.json)

          pnpm --filter @morpho-org/${{ matrix.package }} exec echo "$release_config" > .releaserc

      - run: pnpm --filter @morpho-org/${{ matrix.package }} exec semantic-release
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
