<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@morpho-org/midnight-sdk](./midnight-sdk.md) &gt; [OfferUtils](./midnight-sdk.offerutils.md) &gt; [validateOfferTimeRange](./midnight-sdk.offerutils.validateoffertimerange.md)

## OfferUtils.validateOfferTimeRange() function

Validates and normalizes an offer start/expiry range.

Use this before `Offer.create` when a UI edits dates separately from other offer fields. `Offer.create` calls it internally.

**Signature:**

```typescript
function validateOfferTimeRange(params: {
        readonly start?: BigIntish;
        readonly expiry: BigIntish;
    }): {
        start: bigint;
        expiry: bigint;
    };
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

params


</td><td>

{ readonly start?: [BigIntish](./morpho-ts.bigintish.md)<!-- -->; readonly expiry: [BigIntish](./morpho-ts.bigintish.md)<!-- -->; }


</td><td>


</td></tr>
</tbody></table>

**Returns:**

{ start: bigint; expiry: bigint; }

Normalized start and expiry timestamps.

## Exceptions

`InvalidOfferParameterError` when the range is negative or expiry is before start.

## Remarks

Parameter details:

- `params.start`<!-- -->: Optional offer start timestamp; defaults to zero.

- `params.expiry`<!-- -->: Offer expiry timestamp.

## Example


```ts
import { OfferUtils } from "@morpho-org/midnight-sdk";

const range = OfferUtils.validateOfferTimeRange({ start: 0n, expiry: 1n });
console.log(range.expiry);
```
