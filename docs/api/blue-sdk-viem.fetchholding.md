<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@morpho-org/blue-sdk-viem](./blue-sdk-viem.md) &gt; [fetchHolding](./blue-sdk-viem.fetchholding.md)

## fetchHolding() function

Fetches a user's token holding, allowances, permit nonce, and transfer permission state.

Reads native balances directly for `NATIVE_ADDRESS`<!-- -->. For ERC20 tokens, uses the deployless `GetHolding` query by default and falls back to individual ERC20, Permit2, ERC-2612, Backed, and permissioned-wrapper contract reads when allowed.

**Signature:**

```typescript
export declare function fetchHolding(user: Address, token: Address, client: Client, parameters?: DeploylessFetchParameters): Promise<Holding>;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

user


</td><td>

Address


</td><td>

Address whose holding is fetched.


</td></tr>
<tr><td>

token


</td><td>

Address


</td><td>

Token address, or `NATIVE_ADDRESS` for the native asset.


</td></tr>
<tr><td>

client


</td><td>

Client


</td><td>

Viem client used for deployless reads or multicalls.


</td></tr>
<tr><td>

parameters


</td><td>

[DeploylessFetchParameters](./blue-sdk-viem.deploylessfetchparameters.md)


</td><td>

_(Optional)_


</td></tr>
</tbody></table>

**Returns:**

Promise&lt;[Holding](./blue-sdk.holding.md)<!-- -->&gt;

The hydrated `Holding` entity for `user` and `token`<!-- -->.

## Remarks

Parameter details:

- `parameters.account`<!-- -->: Optional account passed to viem calls.

- `parameters.blockNumber`<!-- -->: Optional block number for historical reads.

- `parameters.blockTag`<!-- -->: Optional block tag for historical reads.

- `parameters.stateOverride`<!-- -->: Optional viem state override.

- `parameters.chainId`<!-- -->: Optional chain id; defaults to `getChainId(client)`<!-- -->.

- `parameters.deployless`<!-- -->: Optional deployless read mode; defaults to `true`<!-- -->.

## Example


```ts
import { NATIVE_ADDRESS, type Holding } from "@morpho-org/blue-sdk";
import { fetchHolding } from "@morpho-org/blue-sdk-viem";
import { createPublicClient, http } from "viem";
import { mainnet } from "viem/chains";

const client = createPublicClient({ chain: mainnet, transport: http() });
const user = "0xBBBBBbbBBb9cC5e90e3b3Af64bdAF62C37EEFFCb";

const holding: Holding = await fetchHolding(user, NATIVE_ADDRESS, client);
```
