<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@morpho-org/midnight-sdk](./midnight-sdk.md) &gt; [EcrecoverRatifierUtils](./midnight-sdk.ecrecoverratifierutils.md) &gt; [typedData](./midnight-sdk.ecrecoverratifierutils.typeddata.md)

## EcrecoverRatifierUtils.typedData() function

Builds EcrecoverRatifier typed data for a tree-like input.

Use after the tree is built and validated, before requesting the signer signature. The EIP-712 verifier is derived from the shared ratifier address on the tree offers. `ratify` calls this for you when given a client and account.

**Signature:**

```typescript
function typedData(params: EcrecoverRatifierTypedDataParams): EcrecoverRatificationTypedData;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

params


</td><td>

[EcrecoverRatifierTypedDataParams](./midnight-sdk.ecrecoverratifiertypeddataparams.md)


</td><td>


</td></tr>
</tbody></table>

**Returns:**

[EcrecoverRatificationTypedData](./midnight-sdk.ecrecoverratificationtypeddata.md)

EIP-712 typed-data descriptor.

## Exceptions

`InvalidTreeError` when the tree is invalid or contains multiple ratifiers.

`InvalidTreeHeightError` when the tree height is unsupported.

## Remarks

Parameter details:

- `params.tree`<!-- -->: Ecrecover-ratified offer tree-like input to sign.

- `params.chainId`<!-- -->: EIP-155 chain id included in the EIP-712 domain.

## Example


```ts
import { EcrecoverRatifierUtils, Tree } from "@morpho-org/midnight-sdk";
import { Offer } from "@morpho-org/midnight-sdk";
import { zeroAddress } from "viem";

const offer = Offer.create({
  market: {
    chainId: 8453,
    midnight: "0x0000000000000000000000000000000000001000",
    loanToken: "0x0000000000000000000000000000000000006000",
    collateralParams: [
      {
        token: "0x0000000000000000000000000000000000007000",
        lltv: 770000000000000000n,
        liquidationCursor: 250000000000000000n,
        oracle: "0x0000000000000000000000000000000000008000",
      },
    ],
    maturity: 54_000n,
    rcfThreshold: 0n,
    enterGate: zeroAddress,
    liquidatorGate: zeroAddress,
  },
  buy: true,
  maker: "0x0000000000000000000000000000000000009000",
  tick: 5_000n,
  expiry: 3_600n,
  ratifier: "0x0000000000000000000000000000000000004000",
  maxUnits: 100n,
});

const typedData = EcrecoverRatifierUtils.typedData({
  tree: Tree.create([offer]),
  chainId: 8453n,
});
console.log(typedData.primaryType);
```
