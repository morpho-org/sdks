<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@morpho-org/midnight-sdk](./midnight-sdk.md) &gt; [TreeUtils](./midnight-sdk.treeutils.md) &gt; [buildProof](./midnight-sdk.treeutils.buildproof.md)

## TreeUtils.buildProof() function

Builds a Merkle proof for one offer.

Use after tree construction when a custom ratifier needs one proof. The built-in Ecrecover and Setter helpers call this while generating `ratifierData`<!-- -->.

**Signature:**

```typescript
function buildProof(params: {
        readonly tree: Pick<TreeLike, "leaves" | "root">;
        readonly leafIndex: BigIntish;
    }): TreeProof;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

params


</td><td>

{ readonly tree: Pick&lt;[TreeLike](./midnight-sdk.treelike.md)<!-- -->, "leaves" \| "root"&gt;; readonly leafIndex: [BigIntish](./morpho-ts.bigintish.md)<!-- -->; }


</td><td>


</td></tr>
</tbody></table>

**Returns:**

[TreeProof](./midnight-sdk.treeproof.md)

Proof descriptor.

## Exceptions

`InvalidTreeError` when leaf index is out of range.

## Remarks

Parameter details:

- `params.tree`<!-- -->: Tree-like data with the root and leaves that contain the leaf.

- `params.leafIndex`<!-- -->: Leaf index to prove.

## Example


```ts
import { Offer, Tree, TreeUtils } from "@morpho-org/midnight-sdk";
import { zeroAddress } from "viem";

const offer = Offer.create({
  market: {
    chainId: 8453,
    midnight: "0x0000000000000000000000000000000000001000",
    loanToken: "0x0000000000000000000000000000000000006000",
    collateralParams: [
      {
        token: "0x0000000000000000000000000000000000007000",
        lltv: 770000000000000000n,
        liquidationCursor: 250000000000000000n,
        oracle: "0x0000000000000000000000000000000000008000",
      },
    ],
    maturity: 54_000n,
    rcfThreshold: 0n,
    enterGate: zeroAddress,
    liquidatorGate: zeroAddress,
  },
  buy: true,
  maker: "0x0000000000000000000000000000000000009000",
  tick: 5_000n,
  expiry: 3_600n,
  ratifier: "0x0000000000000000000000000000000000004000",
  maxUnits: 100n,
});
const proof = TreeUtils.buildProof({
  tree: Tree.create([offer]),
  leafIndex: 0n,
});
console.log(proof.proof.length);
```
