[**@morpho-org/sdks**](../../../../../README.md)

***

[@morpho-org/sdks](../../../../../README.md) / [morpho-ts/src](../../../README.md) / [ChainUtils](../README.md) / CHAIN\_METADATA

# Variable: CHAIN\_METADATA

> `const` **CHAIN\_METADATA**: `object`

Defined in: [packages/morpho-ts/src/chain.ts:156](https://github.com/morpho-org/sdks/blob/000d92bc88f6b9370d16dcc3069ffd81fbd85fe8/packages/morpho-ts/src/chain.ts#L156)

Metadata for each supported chain, keyed by `ChainId`.

## Type Declaration

### 1

> **1**: `object`

#### 1.explorerUrl

> **explorerUrl**: `string` = `"https://etherscan.io"`

#### 1.id

> **id**: [`EthMainnet`](../../../enumerations/ChainId.md#ethmainnet) = `ChainId.EthMainnet`

#### 1.identifier

> **identifier**: `string` = `"mainnet"`

#### 1.name

> **name**: `string` = `"Ethereum"`

#### 1.nativeCurrency

> **nativeCurrency**: `object`

#### 1.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 1.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 1.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 10

> **10**: `object`

#### 10.explorerUrl

> **explorerUrl**: `string` = `"https://optimistic.etherscan.io"`

#### 10.id

> **id**: [`OptimismMainnet`](../../../enumerations/ChainId.md#optimismmainnet) = `ChainId.OptimismMainnet`

#### 10.identifier

> **identifier**: `string` = `"optimism"`

#### 10.name

> **name**: `string` = `"OP Mainnet"`

#### 10.nativeCurrency

> **nativeCurrency**: `object`

#### 10.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 10.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 10.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 1135

> **1135**: `object`

#### 1135.explorerUrl

> **explorerUrl**: `string` = `"https://blockscout.lisk.com"`

#### 1135.id

> **id**: [`LiskMainnet`](../../../enumerations/ChainId.md#liskmainnet) = `ChainId.LiskMainnet`

#### 1135.identifier

> **identifier**: `string` = `"lisk"`

#### 1135.name

> **name**: `string` = `"Lisk"`

#### 1135.nativeCurrency

> **nativeCurrency**: `object`

#### 1135.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 1135.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 1135.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 123420001114

> **123420001114**: `object`

#### 123420001114.explorerUrl

> **explorerUrl**: `string` = `"https://basecamp.cloud.blockscout.com"`

#### 123420001114.id

> **id**: [`CampMainnet`](../../../enumerations/ChainId.md#campmainnet) = `ChainId.CampMainnet`

#### 123420001114.identifier

> **identifier**: `string` = `"camp"`

#### 123420001114.name

> **name**: `string` = `"Camp"`

#### 123420001114.nativeCurrency

> **nativeCurrency**: `object`

#### 123420001114.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 123420001114.nativeCurrency.name

> **name**: `string` = `"Camp"`

#### 123420001114.nativeCurrency.symbol

> **symbol**: `string` = `"CAMP"`

### 130

> **130**: `object`

#### 130.explorerUrl

> **explorerUrl**: `string` = `"https://uniscan.xyz"`

#### 130.id

> **id**: [`Unichain`](../../../enumerations/ChainId.md#unichain) = `ChainId.Unichain`

#### 130.identifier

> **identifier**: `string` = `"unichain"`

#### 130.name

> **name**: `string` = `"Unichain"`

#### 130.nativeCurrency

> **nativeCurrency**: `object`

#### 130.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 130.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 130.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 1329

> **1329**: `object`

#### 1329.explorerUrl

> **explorerUrl**: `string` = `"https://seitrace.com"`

#### 1329.id

> **id**: [`SeiMainnet`](../../../enumerations/ChainId.md#seimainnet) = `ChainId.SeiMainnet`

#### 1329.identifier

> **identifier**: `string` = `"sei"`

#### 1329.name

> **name**: `string` = `"Sei"`

#### 1329.nativeCurrency

> **nativeCurrency**: `object`

#### 1329.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 1329.nativeCurrency.name

> **name**: `string` = `"Sei"`

#### 1329.nativeCurrency.symbol

> **symbol**: `string` = `"SEI"`

### 137

> **137**: `object`

#### 137.explorerUrl

> **explorerUrl**: `string` = `"https://polygonscan.com"`

#### 137.id

> **id**: [`PolygonMainnet`](../../../enumerations/ChainId.md#polygonmainnet) = `ChainId.PolygonMainnet`

#### 137.identifier

> **identifier**: `string` = `"polygon"`

#### 137.name

> **name**: `string` = `"Polygon"`

#### 137.nativeCurrency

> **nativeCurrency**: `object`

#### 137.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 137.nativeCurrency.name

> **name**: `string` = `"Polygon"`

#### 137.nativeCurrency.symbol

> **symbol**: `string` = `"POL"`

### 14

> **14**: `object`

#### 14.explorerUrl

> **explorerUrl**: `string` = `"https://mainnet.flarescan.com"`

#### 14.id

> **id**: [`FlareMainnet`](../../../enumerations/ChainId.md#flaremainnet) = `ChainId.FlareMainnet`

#### 14.identifier

> **identifier**: `string` = `"flare"`

#### 14.name

> **name**: `string` = `"Flare"`

#### 14.nativeCurrency

> **nativeCurrency**: `object`

#### 14.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 14.nativeCurrency.name

> **name**: `string` = `"Flare"`

#### 14.nativeCurrency.symbol

> **symbol**: `string` = `"FLR"`

### 143

> **143**: `object`

#### 143.explorerUrl

> **explorerUrl**: `string` = `"https://monadscan.com"`

#### 143.id

> **id**: [`MonadMainnet`](../../../enumerations/ChainId.md#monadmainnet) = `ChainId.MonadMainnet`

#### 143.identifier

> **identifier**: `string` = `"monad"`

#### 143.name

> **name**: `string` = `"Monad"`

#### 143.nativeCurrency

> **nativeCurrency**: `object`

#### 143.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 143.nativeCurrency.name

> **name**: `string` = `"Monad"`

#### 143.nativeCurrency.symbol

> **symbol**: `string` = `"MON"`

### 146

> **146**: `object`

#### 146.explorerUrl

> **explorerUrl**: `string` = `"https://sonicscan.org"`

#### 146.id

> **id**: [`SonicMainnet`](../../../enumerations/ChainId.md#sonicmainnet) = `ChainId.SonicMainnet`

#### 146.identifier

> **identifier**: `string` = `"sonic"`

#### 146.name

> **name**: `string` = `"Sonic"`

#### 146.nativeCurrency

> **nativeCurrency**: `object`

#### 146.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 146.nativeCurrency.name

> **name**: `string` = `"Sonic"`

#### 146.nativeCurrency.symbol

> **symbol**: `string` = `"S"`

### 16661

> **16661**: `object`

#### 16661.explorerUrl

> **explorerUrl**: `string` = `"https://chainscan.0g.ai"`

#### 16661.id

> **id**: [`ZeroGMainnet`](../../../enumerations/ChainId.md#zerogmainnet) = `ChainId.ZeroGMainnet`

#### 16661.identifier

> **identifier**: `string` = `"0G"`

#### 16661.name

> **name**: `string` = `"0G"`

#### 16661.nativeCurrency

> **nativeCurrency**: `object`

#### 16661.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 16661.nativeCurrency.name

> **name**: `string` = `"0G"`

#### 16661.nativeCurrency.symbol

> **symbol**: `string` = `"0G"`

### 1672

> **1672**: `object`

#### 1672.explorerUrl

> **explorerUrl**: `string` = `"https://pharosscan.xyz"`

#### 1672.id

> **id**: [`PharosMainnet`](../../../enumerations/ChainId.md#pharosmainnet) = `ChainId.PharosMainnet`

#### 1672.identifier

> **identifier**: `string` = `"pharos"`

#### 1672.name

> **name**: `string` = `"Pharos"`

#### 1672.nativeCurrency

> **nativeCurrency**: `object`

#### 1672.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 1672.nativeCurrency.name

> **name**: `string` = `"PharosCoin"`

#### 1672.nativeCurrency.symbol

> **symbol**: `string` = `"PROS"`

### 1868

> **1868**: `object`

#### 1868.explorerUrl

> **explorerUrl**: `string` = `"https://soneium.blockscout.com"`

#### 1868.id

> **id**: [`SoneiumMainnet`](../../../enumerations/ChainId.md#soneiummainnet) = `ChainId.SoneiumMainnet`

#### 1868.identifier

> **identifier**: `string` = `"soneium"`

#### 1868.name

> **name**: `string` = `"Soneium"`

#### 1868.nativeCurrency

> **nativeCurrency**: `object`

#### 1868.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 1868.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 1868.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 200901

> **200901**: `object`

#### 200901.explorerUrl

> **explorerUrl**: `string` = `"https://www.btrscan.com"`

#### 200901.id

> **id**: [`BitlayerMainnet`](../../../enumerations/ChainId.md#bitlayermainnet) = `ChainId.BitlayerMainnet`

#### 200901.identifier

> **identifier**: `string` = `"bitlayer"`

#### 200901.name

> **name**: `string` = `"Bitlayer"`

#### 200901.nativeCurrency

> **nativeCurrency**: `object`

#### 200901.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 200901.nativeCurrency.name

> **name**: `string` = `"Bitcoin"`

#### 200901.nativeCurrency.symbol

> **symbol**: `string` = `"BTC"`

### 21000000

> **21000000**: `object`

#### 21000000.explorerUrl

> **explorerUrl**: `string` = `"https://cornscan.io"`

#### 21000000.id

> **id**: [`CornMainnet`](../../../enumerations/ChainId.md#cornmainnet) = `ChainId.CornMainnet`

#### 21000000.identifier

> **identifier**: `string` = `"corn"`

#### 21000000.name

> **name**: `string` = `"Corn"`

#### 21000000.nativeCurrency

> **nativeCurrency**: `object`

#### 21000000.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 21000000.nativeCurrency.name

> **name**: `string` = `"Bitcoin"`

#### 21000000.nativeCurrency.symbol

> **symbol**: `string` = `"BTCN"`

### 239

> **239**: `object`

#### 239.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.tac.build"`

#### 239.id

> **id**: [`TacMainnet`](../../../enumerations/ChainId.md#tacmainnet) = `ChainId.TacMainnet`

#### 239.identifier

> **identifier**: `string` = `"tac"`

#### 239.name

> **name**: `string` = `"TAC"`

#### 239.nativeCurrency

> **nativeCurrency**: `object`

#### 239.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 239.nativeCurrency.name

> **name**: `string` = `"TAC"`

#### 239.nativeCurrency.symbol

> **symbol**: `string` = `"TAC"`

### 25

> **25**: `object`

#### 25.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.cronos.org"`

#### 25.id

> **id**: [`CronosMainnet`](../../../enumerations/ChainId.md#cronosmainnet) = `ChainId.CronosMainnet`

#### 25.identifier

> **identifier**: `string` = `"cronos"`

#### 25.name

> **name**: `string` = `"Cronos"`

#### 25.nativeCurrency

> **nativeCurrency**: `object`

#### 25.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 25.nativeCurrency.name

> **name**: `string` = `"Cronos"`

#### 25.nativeCurrency.symbol

> **symbol**: `string` = `"CRO"`

### 252

> **252**: `object`

#### 252.explorerUrl

> **explorerUrl**: `string` = `"https://fraxscan.com"`

#### 252.id

> **id**: [`FraxtalMainnet`](../../../enumerations/ChainId.md#fraxtalmainnet) = `ChainId.FraxtalMainnet`

#### 252.identifier

> **identifier**: `string` = `"fraxtal"`

#### 252.name

> **name**: `string` = `"Fraxtal"`

#### 252.nativeCurrency

> **nativeCurrency**: `object`

#### 252.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 252.nativeCurrency.name

> **name**: `string` = `"Frax Ether"`

#### 252.nativeCurrency.symbol

> **symbol**: `string` = `"frxETH"`

### 2741

> **2741**: `object`

#### 2741.explorerUrl

> **explorerUrl**: `string` = `"https://abscan.org"`

#### 2741.id

> **id**: [`AbstractMainnet`](../../../enumerations/ChainId.md#abstractmainnet) = `ChainId.AbstractMainnet`

#### 2741.identifier

> **identifier**: `string` = `"abstract"`

#### 2741.name

> **name**: `string` = `"Abstract"`

#### 2741.nativeCurrency

> **nativeCurrency**: `object`

#### 2741.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 2741.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 2741.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 2818

> **2818**: `object`

#### 2818.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.morphl2.io"`

#### 2818.id

> **id**: [`MorphMainnet`](../../../enumerations/ChainId.md#morphmainnet) = `ChainId.MorphMainnet`

#### 2818.identifier

> **identifier**: `string` = `"morph"`

#### 2818.name

> **name**: `string` = `"Morph"`

#### 2818.nativeCurrency

> **nativeCurrency**: `object`

#### 2818.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 2818.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 2818.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 34443

> **34443**: `object`

#### 34443.explorerUrl

> **explorerUrl**: `string` = `"https://modescan.io"`

#### 34443.id

> **id**: [`ModeMainnet`](../../../enumerations/ChainId.md#modemainnet) = `ChainId.ModeMainnet`

#### 34443.identifier

> **identifier**: `string` = `"mode"`

#### 34443.name

> **name**: `string` = `"Mode"`

#### 34443.nativeCurrency

> **nativeCurrency**: `object`

#### 34443.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 34443.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 34443.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 42161

> **42161**: `object`

#### 42161.explorerUrl

> **explorerUrl**: `string` = `"https://arbiscan.io"`

#### 42161.id

> **id**: [`ArbitrumMainnet`](../../../enumerations/ChainId.md#arbitrummainnet) = `ChainId.ArbitrumMainnet`

#### 42161.identifier

> **identifier**: `string` = `"arbitrum"`

#### 42161.name

> **name**: `string` = `"Arbitrum One"`

#### 42161.nativeCurrency

> **nativeCurrency**: `object`

#### 42161.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 42161.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 42161.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 4217

> **4217**: `object`

#### 4217.explorerUrl

> **explorerUrl**: `string` = `"https://explore.tempo.xyz"`

#### 4217.hasReliableNativeBalance

> **hasReliableNativeBalance**: `false` = `false`

#### 4217.id

> **id**: [`TempoMainnet`](../../../enumerations/ChainId.md#tempomainnet) = `ChainId.TempoMainnet`

#### 4217.identifier

> **identifier**: `string` = `"tempo"`

#### 4217.name

> **name**: `string` = `"Tempo"`

#### 4217.nativeCurrency

> **nativeCurrency**: `object`

#### 4217.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 4217.nativeCurrency.name

> **name**: `string` = `"USD"`

#### 4217.nativeCurrency.symbol

> **symbol**: `string` = `"USD"`

### 42220

> **42220**: `object`

#### 42220.explorerUrl

> **explorerUrl**: `string` = `"https://celoscan.io"`

#### 42220.id

> **id**: [`CeloMainnet`](../../../enumerations/ChainId.md#celomainnet) = `ChainId.CeloMainnet`

#### 42220.identifier

> **identifier**: `string` = `"celo"`

#### 42220.name

> **name**: `string` = `"Celo"`

#### 42220.nativeCurrency

> **nativeCurrency**: `object`

#### 42220.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 42220.nativeCurrency.name

> **name**: `string` = `"Celo"`

#### 42220.nativeCurrency.symbol

> **symbol**: `string` = `"CELO"`

### 42793

> **42793**: `object`

#### 42793.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.etherlink.com"`

#### 42793.id

> **id**: [`EtherlinkMainnet`](../../../enumerations/ChainId.md#etherlinkmainnet) = `ChainId.EtherlinkMainnet`

#### 42793.identifier

> **identifier**: `string` = `"etherlink"`

#### 42793.name

> **name**: `string` = `"Etherlink"`

#### 42793.nativeCurrency

> **nativeCurrency**: `object`

#### 42793.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 42793.nativeCurrency.name

> **name**: `string` = `"Tezos"`

#### 42793.nativeCurrency.symbol

> **symbol**: `string` = `"XTZ"`

### 43111

> **43111**: `object`

#### 43111.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.hemi.xyz"`

#### 43111.id

> **id**: [`HemiMainnet`](../../../enumerations/ChainId.md#hemimainnet) = `ChainId.HemiMainnet`

#### 43111.identifier

> **identifier**: `string` = `"hemi"`

#### 43111.name

> **name**: `string` = `"Hemi"`

#### 43111.nativeCurrency

> **nativeCurrency**: `object`

#### 43111.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 43111.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 43111.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 4326

> **4326**: `object`

#### 4326.explorerUrl

> **explorerUrl**: `string` = `"https://mega.etherscan.io"`

#### 4326.id

> **id**: [`MegaEthMainnet`](../../../enumerations/ChainId.md#megaethmainnet) = `ChainId.MegaEthMainnet`

#### 4326.identifier

> **identifier**: `string` = `"megaeth"`

#### 4326.name

> **name**: `string` = `"MegaETH"`

#### 4326.nativeCurrency

> **nativeCurrency**: `object`

#### 4326.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 4326.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 4326.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 4663

> **4663**: `object`

#### 4663.explorerUrl

> **explorerUrl**: `string` = `"https://robinhoodchain.blockscout.com"`

#### 4663.id

> **id**: [`RobinhoodMainnet`](../../../enumerations/ChainId.md#robinhoodmainnet) = `ChainId.RobinhoodMainnet`

#### 4663.identifier

> **identifier**: `string` = `"robinhood"`

#### 4663.name

> **name**: `string` = `"Robinhood Chain"`

#### 4663.nativeCurrency

> **nativeCurrency**: `object`

#### 4663.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 4663.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 4663.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 480

> **480**: `object`

#### 480.explorerUrl

> **explorerUrl**: `string` = `"https://worldscan.org"`

#### 480.id

> **id**: [`WorldChainMainnet`](../../../enumerations/ChainId.md#worldchainmainnet) = `ChainId.WorldChainMainnet`

#### 480.identifier

> **identifier**: `string` = `"worldchain"`

#### 480.name

> **name**: `string` = `"World Chain"`

#### 480.nativeCurrency

> **nativeCurrency**: `object`

#### 480.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 480.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 480.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 50

> **50**: `object`

#### 50.explorerUrl

> **explorerUrl**: `string` = `"https://xdcscan.com"`

#### 50.id

> **id**: [`XdcMainnet`](../../../enumerations/ChainId.md#xdcmainnet) = `ChainId.XdcMainnet`

#### 50.identifier

> **identifier**: `string` = `"xdc"`

#### 50.name

> **name**: `string` = `"XDC"`

#### 50.nativeCurrency

> **nativeCurrency**: `object`

#### 50.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 50.nativeCurrency.name

> **name**: `string` = `"XDC"`

#### 50.nativeCurrency.symbol

> **symbol**: `string` = `"XDC"`

### 5042

> **5042**: `object`

#### 5042.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.arc.io"`

#### 5042.id

> **id**: [`ArcMainnet`](../../../enumerations/ChainId.md#arcmainnet) = `ChainId.ArcMainnet`

#### 5042.identifier

> **identifier**: `string` = `"arc"`

#### 5042.name

> **name**: `string` = `"Arc"`

#### 5042.nativeCurrency

> **nativeCurrency**: `object`

#### 5042.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 5042.nativeCurrency.name

> **name**: `string` = `"USDC"`

#### 5042.nativeCurrency.symbol

> **symbol**: `string` = `"USDC"`

### 534352

> **534352**: `object`

#### 534352.explorerUrl

> **explorerUrl**: `string` = `"https://scrollscan.com"`

#### 534352.id

> **id**: [`ScrollMainnet`](../../../enumerations/ChainId.md#scrollmainnet) = `ChainId.ScrollMainnet`

#### 534352.identifier

> **identifier**: `string` = `"scroll"`

#### 534352.name

> **name**: `string` = `"Scroll"`

#### 534352.nativeCurrency

> **nativeCurrency**: `object`

#### 534352.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 534352.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 534352.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 56

> **56**: `object`

#### 56.explorerUrl

> **explorerUrl**: `string` = `"https://bscscan.com"`

#### 56.id

> **id**: [`BscMainnet`](../../../enumerations/ChainId.md#bscmainnet) = `ChainId.BscMainnet`

#### 56.identifier

> **identifier**: `string` = `"bsc"`

#### 56.name

> **name**: `string` = `"Bnb Smart Chain"`

#### 56.nativeCurrency

> **nativeCurrency**: `object`

#### 56.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 56.nativeCurrency.name

> **name**: `string` = `"Bnb"`

#### 56.nativeCurrency.symbol

> **symbol**: `string` = `"BNB"`

### 57073

> **57073**: `object`

#### 57073.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.inkonchain.com"`

#### 57073.id

> **id**: [`InkMainnet`](../../../enumerations/ChainId.md#inkmainnet) = `ChainId.InkMainnet`

#### 57073.identifier

> **identifier**: `string` = `"ink"`

#### 57073.name

> **name**: `string` = `"Ink"`

#### 57073.nativeCurrency

> **nativeCurrency**: `object`

#### 57073.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 57073.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 57073.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 59144

> **59144**: `object`

#### 59144.explorerUrl

> **explorerUrl**: `string` = `"https://lineascan.build"`

#### 59144.id

> **id**: [`LineaMainnet`](../../../enumerations/ChainId.md#lineamainnet) = `ChainId.LineaMainnet`

#### 59144.identifier

> **identifier**: `string` = `"linea"`

#### 59144.name

> **name**: `string` = `"Linea"`

#### 59144.nativeCurrency

> **nativeCurrency**: `object`

#### 59144.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 59144.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 59144.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 685689

> **685689**: `object`

#### 685689.explorerUrl

> **explorerUrl**: `string` = `"https://gensyn-mainnet.explorer.alchemy.com"`

#### 685689.id

> **id**: [`GensynMainnet`](../../../enumerations/ChainId.md#gensynmainnet) = `ChainId.GensynMainnet`

#### 685689.identifier

> **identifier**: `string` = `"gensyn"`

#### 685689.name

> **name**: `string` = `"Gensyn"`

#### 685689.nativeCurrency

> **nativeCurrency**: `object`

#### 685689.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 685689.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 685689.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 714

> **714**: `object`

#### 714.explorerUrl

> **explorerUrl**: `string` = `"https://eden.blockscout.com"`

#### 714.id

> **id**: [`EdenMainnet`](../../../enumerations/ChainId.md#edenmainnet) = `ChainId.EdenMainnet`

#### 714.identifier

> **identifier**: `string` = `"eden"`

#### 714.name

> **name**: `string` = `"Eden"`

#### 714.nativeCurrency

> **nativeCurrency**: `object`

#### 714.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 714.nativeCurrency.name

> **name**: `string` = `"TIA"`

#### 714.nativeCurrency.symbol

> **symbol**: `string` = `"TIA"`

### 747474

> **747474**: `object`

#### 747474.explorerUrl

> **explorerUrl**: `string` = `"https://katanascan.com"`

#### 747474.id

> **id**: [`KatanaMainnet`](../../../enumerations/ChainId.md#katanamainnet) = `ChainId.KatanaMainnet`

#### 747474.identifier

> **identifier**: `string` = `"katana"`

#### 747474.name

> **name**: `string` = `"Katana"`

#### 747474.nativeCurrency

> **nativeCurrency**: `object`

#### 747474.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 747474.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 747474.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 8217

> **8217**: `object`

#### 8217.explorerUrl

> **explorerUrl**: `string` = `"https://kaiascan.io"`

#### 8217.id

> **id**: [`KaiaMainnet`](../../../enumerations/ChainId.md#kaiamainnet) = `ChainId.KaiaMainnet`

#### 8217.identifier

> **identifier**: `string` = `"kaia"`

#### 8217.name

> **name**: `string` = `"Kaia"`

#### 8217.nativeCurrency

> **nativeCurrency**: `object`

#### 8217.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 8217.nativeCurrency.name

> **name**: `string` = `"Kaia"`

#### 8217.nativeCurrency.symbol

> **symbol**: `string` = `"KAIA"`

### 8453

> **8453**: `object`

#### 8453.explorerUrl

> **explorerUrl**: `string` = `"https://basescan.org"`

#### 8453.id

> **id**: [`BaseMainnet`](../../../enumerations/ChainId.md#basemainnet) = `ChainId.BaseMainnet`

#### 8453.identifier

> **identifier**: `string` = `"base"`

#### 8453.name

> **name**: `string` = `"Base"`

#### 8453.nativeCurrency

> **nativeCurrency**: `object`

#### 8453.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 8453.nativeCurrency.name

> **name**: `string` = `"Ether"`

#### 8453.nativeCurrency.symbol

> **symbol**: `string` = `"ETH"`

### 988

> **988**: `object`

#### 988.explorerUrl

> **explorerUrl**: `string` = `"https://stablescan.xyz"`

#### 988.id

> **id**: [`StableMainnet`](../../../enumerations/ChainId.md#stablemainnet) = `ChainId.StableMainnet`

#### 988.identifier

> **identifier**: `string` = `"stable"`

#### 988.name

> **name**: `string` = `"Stable"`

#### 988.nativeCurrency

> **nativeCurrency**: `object`

#### 988.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 988.nativeCurrency.name

> **name**: `string` = `"USDT0"`

#### 988.nativeCurrency.symbol

> **symbol**: `string` = `"USDT0"`

### 98866

> **98866**: `object`

#### 98866.explorerUrl

> **explorerUrl**: `string` = `"https://explorer.plume.org"`

#### 98866.id

> **id**: [`PlumeMainnet`](../../../enumerations/ChainId.md#plumemainnet) = `ChainId.PlumeMainnet`

#### 98866.identifier

> **identifier**: `string` = `"plume"`

#### 98866.name

> **name**: `string` = `"Plume"`

#### 98866.nativeCurrency

> **nativeCurrency**: `object`

#### 98866.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 98866.nativeCurrency.name

> **name**: `string` = `"Plume"`

#### 98866.nativeCurrency.symbol

> **symbol**: `string` = `"PLUME"`

### 999

> **999**: `object`

#### 999.explorerUrl

> **explorerUrl**: `string` = `"https://hyperevmscan.io"`

#### 999.id

> **id**: [`HyperliquidMainnet`](../../../enumerations/ChainId.md#hyperliquidmainnet) = `ChainId.HyperliquidMainnet`

#### 999.identifier

> **identifier**: `string` = `"hyperliquid"`

#### 999.name

> **name**: `string` = `"Hyperliquid"`

#### 999.nativeCurrency

> **nativeCurrency**: `object`

#### 999.nativeCurrency.decimals

> **decimals**: `number` = `18`

#### 999.nativeCurrency.name

> **name**: `string` = `"Hype"`

#### 999.nativeCurrency.symbol

> **symbol**: `string` = `"HYPE"`
