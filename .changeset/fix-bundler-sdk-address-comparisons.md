---
"@morpho-org/bundler-sdk-viem": patch
---

Compare Bundler3 action addresses case-insensitively so non-checksummed addresses compute native bundle value and native-transfer short-circuits correctly.
