---
"@morpho-org/wdk-protocol-lending-morpho-evm": patch
---

Refresh the WDK wallet runtime dependencies and the shared SDK development toolchain, including the compatibility patch for the updated viem dependency graph.
