---
"@morpho-org/blue-sdk": patch
"@morpho-org/blue-sdk-viem": patch
"@morpho-org/liquidity-sdk-viem": patch
"@morpho-org/wdk-protocol-lending-morpho-evm": patch
---

Refresh non-deprecated SDK dependencies and keep the fork-test/tooling compatibility fixes needed by the updated stack.

- Update WDK runtime dependencies to the latest non-deprecated compatible releases.
- Update Biome no-shadow suppressions after the rule moved from nursery to suspicious in Biome 2.5.
- Patch the transitive `ox@0.14.29` Tempo authorization return type so clean TypeScript builds pass with `viem@2.52.2`.
- Keep RPC fork tests deterministic under the updated Anvil/Vitest stack by disabling Anvil storage caching in non-deprecated fork-test fixtures and widening slow fork-test timeouts where needed.
