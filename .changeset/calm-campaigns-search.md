---
"@morpho-org/morpho-ts": minor
---

Deprecate unused `WithId` and `WithIndex` utility types, deprecate legacy CDN and optimizer URL constants, and point `REWARDS_BASE_URL` to the campaigns subdomain.
