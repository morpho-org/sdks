import { OfferTreeUtils } from "./OfferTreeUtils.js";

/**
 * Object-compatible tree hashing, root, proof, and verification helpers.
 *
 * @example
 * ```ts
 * import { TreeUtils } from "@morpho-org/midnight-sdk";
 *
 * console.log(typeof TreeUtils.buildOfferTreeRoot);
 * ```
 */
export const TreeUtils = OfferTreeUtils;
