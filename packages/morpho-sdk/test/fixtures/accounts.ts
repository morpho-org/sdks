/**
 * First Anvil default private key (account `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`).
 * Pinned for tests that only need a wallet client to exercise sign-time
 * validation paths — never to produce real signatures on a real network.
 */
export const TEST_PRIVATE_KEY =
  "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80";
